package com.telegram.xmasstree_bot.server.service.strategy

/**
 * StrategyType enum class.
 * Used to represent a strategy type.
 */
enum class StrategyType {
    TEXT,
    PHOTO,
    LOCATION,
    COMMAND,
    CALLBACK
}